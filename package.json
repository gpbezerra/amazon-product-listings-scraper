{
  "name": "amazon-product-listings-scraper",
  "private": true,
  "scripts": {
    "dev": "(cd backend && bun --watch src/index.ts) & (cd frontend && bun run dev) & wait",
    "install:all": "cd backend && bun install && cd ../frontend && bun install"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}